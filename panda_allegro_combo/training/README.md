All work being done right now is in training. You can start by running each of the files until you get 100%, just change your resume_path location to match where you want to start each time to achieve the most efficient route to 100% success.

Run `python pretrain.py` and then `python train.py` to train the model.

Optional: run `render_train.py` to render the training you have done so far.

Then run `python hold_train.py`

Lastly, run `python grasp_train.py`
